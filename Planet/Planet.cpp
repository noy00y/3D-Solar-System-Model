#include "Planet.hpp"

Planet::Planet(float _radius, float _distance, float _orbit, float _speed, float _tilt, float _axis){
	radius = _radius;
	distance = _distance;
	orbit = _orbit;
	speed = _speed;
	tilt = _tilt;
	axis = _axis;
}
